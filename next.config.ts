import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  images: {
    domains: [
      'i.pinimg.com',
      // 'images.unsplash.com',
      // 'cdn.discordapp.com',
      // 'pbs.twimg.com',
      // 'example-cdn.com',
    ],
  },
  /* config options here */
}

export default nextConfig
